name: regula
description: "Cross-platform dynamic geometry app (construction-graph based)."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  file_picker: ^12.0.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  riverpod_annotation: ^4.0.0
  shared_preferences: ^2.5.5
  uuid: ^4.5.3

dev_dependencies:
  build_runner: ^2.6.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  freezed: ^3.0.0
  glados: ^1.1.0
  json_serializable: ^6.9.0
  mocktail: ^1.0.0
  riverpod_generator: ^4.0.0

flutter:
  uses-material-design: true
