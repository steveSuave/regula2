# Status Log

Append-only journal of working sessions. **Entries are appended at the end — the newest is last** (the convention since session 163; the archives before that are newest-first). Each entry should answer three questions in 5–15 lines: **what was done**, **what's next**, **gotchas / open questions**.

Write a fresh entry at the end of every session, before stopping. Do not edit older entries — if something turned out wrong, note it in the next entry.

Rotation: keep roughly the last 10 sessions here; move older entries to `docs/archive/` every 20–30 sessions. Archives: V1's log in `STATUS-sessions-01-88.md` and `STATUS-sessions-89-98.md`; V2's in `STATUS-sessions-99-107.md` and `STATUS-sessions-108-175.md` (rotated 2026-09-03). Sessions only need the newest 2–3 entries — don't read the archive unless hunting for old history.

---

## 2026-08-25 — session 176: Phase 153, the rig before the search

**Done — two commits on `phase-153-auxiliary-construction`, four of the phase's seven boxes.** The measurement A2's checklist asked for ("a search, so it needs a budget and an order, both measured rather than chosen"), taken before the search that would use it — Phases 151b and 152e's shape a fifth time.

- **`lib/domain/prover/auxiliary_points.dart`** — the candidate list, in `domain/` because both the measurement and any future engine read it and writing it twice would let them drift. Three families in the checklist's order, deterministic over the objects' own order, so index *i* names the same point in every copy of a document. **The dedup is numeric and that is the decision**: a document already naming the midpoint of `AB` is a structural coincidence, but the foot of a centre on a chord being that chord's midpoint is a *theorem*, and no structural test sees the second kind. That is the `DiagramFilter` boundary, not a breach of it — proposing is a search heuristic, and nothing there asserts a predicate. A structural incidence skip was written first and **deleted**: mutation-checking showed it failed no test the dedup was not already failing.
- **The result: 2 unlocks of 1 fact from 276 candidates over seven documents.** Both on `perp-true-unproved.rgl`, both midpoints, both reaching `perp(C,D,D,F)` — the midpoint of `BC`, which is the point JGEX's dialog names, and the midpoint of `BF` on the same line. The proof verifies and cites the invented point. So A2 is exactly the mechanism that document needs and **nothing else in the corpus needs it**, which is Phase 152e's corpus finding again rather than an exception to it.
- **The metric correction is most of the answer.** A first pass compared fact sets and reported unlocks on six of seven fixtures — every one a re-spelling. Adding *any* point to `no-locus.rgl` "unlocks" `para(F,G,F,B)`, which is the hypothesis `coll(F,G,B)` said in `para` language: the angle closure entails it and declines to *publish* it, a `para` sharing a point being a degeneracy. The screen is `Prover.resolve`, the question a user's *Ask* already goes through. Pinned as its own test, because reading the fact set would have made every number in the phase wrong by an order of magnitude.
- **Order settled, budget opened.** The 87 feet and 45 intersections unlock nothing anywhere, so midpoints-first is a measurement now. And a candidate costs a *full re-run*, so an exhaustive pass is (candidates) × (baseline): 78 × 643 ms = **50 s on `provoleas2.json`** against its own 368 ms, worst single candidate **4.4 s**. The probe is 2 ms of that 50 s; the exchange is the cost. No ordering makes an exhaustive sweep cheap — what an ordering buys is finding the point *early*, which is the claim a search has to be built on.
- **Cost split by document, deliberately.** `provoleas2` and `tangent-chase` are 67 of the sweep's 74 seconds, so the exhaustive form and the timings live in `benchmark/auxiliary_search_bench.dart` (timings not being a thing to assert) and the suite carries the other five in 12 s. Their negative was measured at a 200 000 cap with every candidate run reaching quiescence.

Gates: analyze clean, suite **3352** green (3342 at the phase's start), 50 s wall clock.

**Next.** The remaining three boxes: the search itself (goal-directed, early-exit, bounded in `runChunked`'s currency rather than a fact count), the modal that names an invented point and offers to add it, and a replacement *unproved* rig. Carried: `readFact` on listed `eqangle`s can still read magnitude-false; Phase 157's and the builder's browser looks unverified; Phase 161's decision box.

**Gotchas.**

- **`Set == Set` is identity in Dart**, and it cost a debugging round in a test that looked obviously right. Compare sorted lists, or joined keys.
- **A "new fact" is not new content**, and the whole first pass of this measurement was wrong for that reason. Anything comparing two prover runs should ask the *engine* whether the difference is entailed, not diff the fact set — the closures deliberately withhold degenerate spellings, so the fact set understates what a run knows.
- **`o.circle != null` and `o.conic != null` are different filters** and the candidate count moves by two on `provoleas2` between them; the projective accessor is the one domain code reads, per CLAUDE.md, and it is the one in `lib/`. The scratch rig used the affine getter and under-counted.
- The sweep is **one point at a time**. A theorem needing two auxiliary points comes back as a zero here, and JGEX constructs more than one on harder documents — stated in the test's header so the negative is not read as broader than it is.

## 2026-08-25 — session 176 (continued): Phase 153 complete

**Done — Phase 153 end to end, six commits on `phase-153-auxiliary-construction`.** The measurement (above), then the search the measurement designed, then the ask and the panel.

- **`auxiliary_search.dart`** — goal-directed with early exit, and **resumable by cursor** on `ProverEngine`'s Phase 140 precedent, because an attempt is a whole document run. It takes the question's *spellings* and reports which it reached — they are one statement, and the proof has to be read off the spelling the run derived. Nothing is mutated: each attempt runs against the document's objects plus one **detached** point, so the goal keeps naming the user's real points. `reached` is `Prover.resolve`, not membership.
- **The ordering claim, measured**: JGEX's midpoint of `BC` is candidate **4 of 26** on the one document with an answer, so early exit costs five runs rather than twenty-six. That is what an order buys — not a cheaper sweep, an earlier answer.
- **`ProverNotifier.searchForPoint`**, guarded on exactly one verdict and **user-initiated, never automatic**. *Undecided* has shown nothing about reachability and wants another budget, not a hundred runs. Progress counts *candidates*, not applications. The run behind an invented point is not published — it holds facts about a figure the user does not have.
- **The panel offers it and says what it built.** The proposal carries the document's own next automatic point name, so a step reads `perp(F, D, D, G)` and accepting it is an `AddObjectCommand` and nothing else. The disclosure sits *above* the proof, where the name it introduces is next to the steps that use it. Once a search has tried everything the offer withdraws and the message strengthens.
- **Box 7's premise was wrong, and that is the finding.** It assumed the search would run as part of asking. It does not, so `perp-true-unproved.rgl` keeps its role as the *unproved* rig and Phase 148's three-verdict tests are untouched — the fixture is now the rig for both halves. What the box got right is that its stopgap is spent: `tangent-chase.rgl`'s `cong(P,S,P,T)` is stored in the baseline run since Phase 155.

Gates: analyze clean, suite **3369** green (3342 at the phase's start), browser gate **15**.

**Next.** Phase 161's decision box is the remaining engine item; the corpus is still the limiting factor, which five measurements now say. Carried: `readFact` on listed `eqangle`s can still read magnitude-false; Phase 157's and the builder's browser looks unverified; the id-based fact transfer for `Isolate.run`.

**Gotchas.**

- **The search is one point at a time**, and a theorem needing two comes back exhausted. Named in TODO so the negative is not read as broader than it is.
- **`Prover.resolve` records what it confirms**, so a membership test *after* an ask sees the fact either way. A test that means to pin "the closure answered it" must assert the step's *rule*, not the fact's absence. Cost one debugging round.
- The panel's new strings are plain ASCII, unlike the chase's `θ`/`π`/`⟹`, so nothing here needed a browser-gate entry — but the gate was run and is green at 15.
- **None of this has been looked at in a browser.** The flow is pinned in `testWidgets`, and driving it live needs the JGEX document through the file picker, which browser automation cannot do.

## 2026-08-25 — session 177: Phase 167, the corpus and the first honest baseline

**What was done.** A user asked for JGEX's and Newclid's provers to be reverse-engineered and worthwhile features ported, tested hard enough not to import their bugs. Both were read end to end. The port that came out of it is not a feature: it is the **benchmark**, because PLAN ends five separate measurements with "the corpus is the limiting factor, not the algebra" and every one of those was an inference from seven documents.

`test/corpus/newclid_problem.dart` parses Newclid's one-line construction DSL; `test/corpus/newclid_translation.dart` builds it as a `Construction` over 28 macros mapped onto object kinds that already exist; `benchmark/corpus_bench.dart` + `run_corpus.sh` run the lot at the provider's own numbers. **No `lib/` change.** Baseline: **928 goals, 466 built, 46 proved, 401 unproved, 19 undecided, 0 refuted**, 119 s, **0 unsound proofs**. Written up in PLAN §"The prover against a real benchmark"; TODO's Phase 167 has the boxes.

**What's next.** The benchmark now dictates the order, and each phase is kept or deleted on what it moves: the **carrier publisher** (`coll` is 1 of 59, and PLAN predicted exactly this — "no angle algebra can publish it"), then the ~18 in-vocabulary rules Newclid has and this table lacks (`cyclic` is **0 of 43**), then the direct/reflected split with `sameclock`, then constants. `imo_102_requires_aux.txt` is 44 built / 0 proved — the rig Phase 153's auxiliary search never had.

**Gotchas.**

- **"Refuted" was a claim the run had not earned.** Four problems came back refuted; all four are figures that *came apart* — a second, independently seeded perturbation carries a point across a branch, the intersection swaps, and the problem's own `coll` hypothesis stops holding. They are `unstableFigure` now and the baseline's refutation count is zero. **A perturbation probe and a branch-addressed construction interact**, and 4 in 466 is the first scale at which that is visible. Worth remembering the next time a filter refuses something surprising.
- **The first mutation check passed, and that meant the test was too weak, not the rule redundant.** Disabling "a new point never lands on a point the figure already names" failed nothing — then the corpus said the rule is worth **86 of the 466** problems that build. `test_get_two_intersections` is the shape that pins it. A mutation that survives is a question, not a verdict.
- **A silently-wrong translation is worse than a refusal**, which is why `on_aline` (57 uses, the largest unread macro) is refused rather than faked: "the line through P at a copied angle" is an object `lib/domain/construction/` lacks, and building it by numeric rotation would produce a figure that looks right and whose defining hypothesis `hypotheses()` could not see — that function reads parent ties, not positions.
- **The corpus is not vendored.** `--corpus` points at a checkout of Newclid; `test/fixtures/` stays v1's permanent save-format corpus, and the gate's fifteen problems are inlined verbatim in the test file.
- **Correcting session 176's entry**, per this file's own rule about not editing older ones: its gotcha said "None of this has been looked at in a browser". That was true when written and is not now — Phase 153's auxiliary search was driven end to end on the deployed wasm build, the *unproved* verdict, the **Look for a point that would settle it** row, the disclosure card naming G as the midpoint of B, C, and **Add it** putting G on segment CB.

## 2026-08-26 — session 178: Phase 168, the carrier publisher — measured, not built

**Done — the corpus benchmark's own ranking, checked before writing any code, and it does not hold up.** PLAN's ordering put the carrier publisher first: `coll` proved on 1 of 59 built corpus goals, and `CarrierIndex` (Phase 151a) already knows the incidence closure `coll_transitive`/`perp_coll`/`cyclic_fifth_point` compute one rule application at a time. `docs/PLAN.md` and `docs/TODO.md` updated; no `lib/` or `test/` change, so no suite/gate numbers move.

- **The coverage question: 0 of 52.** Every corpus `coll` goal that finished quiescent and unproved was asked of a fresh `CarrierIndex` built over that run's own final fact set — the strongest form of the question, since the closure sees everything DD/AR derived. Not one comes back yes that the database does not already hold. The reason is structural rather than incidental: `coll_transitive` merges on a shared *pair* of points, the same soundness trigger `CarrierIndex.absorb` merges on, so a fixpoint of one is the fixpoint of the other by construction.
- **The cost question, checked because PLAN's actual claim was "what a future deletion of `coll_transitive` rests on", not "more coverage".** The three incidence rules derive 5.6% of all facts corpus-wide (1 503 of 26 610 across 466 built problems); 12 of the corpus's 19 undecided problems touch them, but none shows the dominant share `eqangle_transitive` had before Phase 163 deleted it. No budget win to point at either.
- **Measured with two throwaway scripts** (`benchmark/_scratch_coll_probe.dart`, `_scratch_coll_cost.dart`), run against the Newclid checkout and deleted afterward — the harness was the answer, not a deliverable, on the `para_coll` precedent.

**Next.** The benchmark's own ordering, unchanged past this item: the ~18 in-vocabulary rules Newclid has that this table lacks, `cyclic` first (0 of 43 proved) — R01 (`cong` ×3 from a centre), R04 (inscribed converse with an `ncoll` guard), R49/R50 (recognising a centre). Then the direct/reflected `simtri` split (`sameclock`), then constants. `imo_102_requires_aux.txt` (44 built, 0 proved) is still the rig Phase 153's auxiliary search never had.

**Gotchas.** STATUS.md's header still says "newest entries on top"; actual practice for a long stretch of sessions has been appending at the bottom instead (this entry follows that practice, not the header). Worth fixing the header text itself sometime rather than leaving the two disagree.

## 2026-08-26 — session 178 (continued): Phase 169, the rule the corpus actually wanted

**Done — one rule added, one measured and dropped, +16 proved goals on the corpus.** Straight continuation of this session's Phase 168 work: the benchmark's ordering names "`cyclic` first" as the next item after the carrier publisher, so it was next. `lib/domain/prover/rule.dart`, `test/domain/prover/rule_engine_test.dart`, `test/domain/prover/rule_test.dart`.

- **`equidistant_cyclic`, Newclid's R01, is the rule.** `cong(o,a,o,b) & cong(o,b,o,c) & cong(o,c,o,d) => cyclic(a,b,c,d)` — literally the definition of concyclic. `inscribed_converse` (R04) was already in the table; R01 was not, and it is the one four-points-equidistant-by-other-reasoning problems actually need, since a structural circle's centre already gives `hypotheses` the same `cong` chain for free.
- **Zero on the seven fixtures, +16 on the corpus — which is Phase 167's whole argument, restated with a number attached to it a second time.** `benchmark/corpus_bench.dart`: **46 → 62 proved** of 466 built. One direct `cyclic`, and fifteen indirect — `inscribed_angle` reads the new `cyclic` facts into `eqangle` (13 → 20 proved), the angle closure reads those into `para` (10 → 18 proved). `coll` and `perp` did not move, which is the rule behaving exactly as narrow as its premises. 0 unsound (`Proof.verify()` on every proved goal), 0 refuted, undecided **unchanged at 19** — no problem tipped past budget.
- **R50 was tried alongside it and dropped.** `cyclic(a,b,c,d) & cong(o,a,o,b) & cong(o,c,o,d) => cong(o,a,o,c)` (partial-evidence centre recognition): +1 proved, **+3 undecided**. `cyclic`'s orbit is large, so a second rule taking it as a premise is expensive, and here the expense outweighs the one extra proof. Measured, written into the rule's own doc comment, reverted before committing.
- **Gates: analyze clean, suite 3401 (3400 + 1).** Fixture suite unchanged confirms the zero-fixtures-effect claim rather than assuming it.

**Next.** `cyclic`'s other 42 unproved goals, and the rest of the ~18-rule estimate the reverse-engineering pass named — R49 in cong-only form is still untried (subsumed by R50's negative result rather than tested on its own). Then the direct/reflected `simtri` split (`sameclock`), then constants — the benchmark's ordering, unchanged past this item.

**Gotchas.** The corpus benchmark takes 2–4 minutes wall per run and its wall-clock number is noisy across runs (176 s, 209 s and 213 s were seen for what should be the same or near-identical rule tables, likely machine load) — trust the *counts* (proved/undecided/refuted), not the wall time, when comparing two runs on this machine.

## 2026-08-28 — session 179: Phase 172, the direct/reflected split — measured, not built

**Done — one commit on `phase-172-sameclock`, phase closed as a measured negative.** The benchmark's ordering named the `simtri` split with `sameclock` as the next and largest remaining lever, and `rule.dart` had carried it as a to-do since M-P1. No `lib/` change beyond the measurement recorded at the site.

- **Measuring it needed no new predicate, and that is what made the phase cheap.** Newclid's `sameclock` is a `NumericalPredicate` (`predicates/sameclock.py`) — checked against the diagram, never derived — the same species as the `ncoll` side conditions `rule.dart`'s header already delegates to the `DiagramFilter`. So both arms of R52/R53 (three `eqangle` each, direct and reflected) were handed to the engine and the screen left to keep whichever holds. That *is* the split, modulo the proof citing its guard, so reachability is measured exactly rather than bounded.
- **0 goals of 383**, over every corpus goal that finished quiescent and unproved, re-run from its own stored fixpoint (Phase 171's confluence trick; the 19 budget-exhausted excluded). Not one fact credited to any of the six rules.
- **They do fire — checked directly, per Phase 170's correction.** On all **40** `simtri`/`contri` facts those fixpoints hold. Of the 2 880 conclusion-instances that produces over the full 12-element orbit, **1 440 are already in the database and 1 440 are refused by the screen** — an exact halving, and the reason is the finding: *the angle set a similarity would republish is the set `aa_simtri` matched in order to derive it*. The reflected arm is the numerically false half, every time.
- **The pre-registered control is 0 too.** R52's remaining orientation-free `eqratio` needs no guard at all; it moves nothing and derives nothing new. So what is missing from `simtri`'s output is not orientation — the output set is saturated.
- **The supply is the real number: `simtri`/`contri` appear at all in 11 of those 383 problems.** The premise side cannot be the fix either, since this table's three similarity criteria are orientation-*free* and so fire strictly more often than Newclid's guarded R34/R36/R60 pairs — splitting them can only lower the count. On the goal side the split would make **5 corpus goals of 928** statable (`simtrir` 3, `contrir` 2); the 15 `sameclock` goals would be the screen replying, not the prover.

Gates: analyze clean, suite unchanged at **3402**.

**Next.** Constants (`aconst` 12 corpus goals, `rconst` 9, `lconst` 6) are what is left of the benchmark's ordering. But four of the six levers it named are now measured and three came back zero — carrier publisher, R21, `eqangle`'s consumer set bar R30, and this — against R01's +16 and R30's +2. Each asked "which rule is missing", and the answer each time was that the table can already state what the goal needs. `cong` is 259 of 928 corpus goals and `coll` 131; the honest next question may be what the 383 quiescent runs *are* missing, which is a question about the shape of the search rather than the size of the table. Carried: `readFact` on listed `eqangle`s can still read magnitude-false; Phase 161's decision box; `imo_102_requires_aux.txt` (44 built, 0 proved) is still the rig Phase 153's auxiliary search never had.

**Gotchas.**

- **This file has no entries for Phases 170 and 171.** Both are merged on `main` and fully ticked in `docs/TODO.md`; the sessions that shipped them wrote no STATUS entry. Nothing is reconstructed here — read those two phases' TODO sections and commit bodies (`1836bde`, `4510198`) for what they found. The header's own claim that entries go newest-on-top has also been false for a long stretch; practice is to append, and this entry follows practice.
- **The probe ran the whole corpus three times at 233–450 s a pass**, and the two 450 s arms differ from the 233 s one only in doing the wide re-runs. Budget ~8 minutes per arm before starting, and prefer a classification pass (which skips the re-run) when the question is "did the rule fire" rather than "did a goal move".
- **A rule that fires and adds nothing looks identical to a rule that never fires**, from the credited-fact count alone — the count is zero either way, because `FactDatabase.add` credits only what is new. Classifying each candidate conclusion as present / screened / new is what separates them, and here it turned a flat zero into the phase's actual finding.

## 2026-08-28 — session 179 (continued): Phase 173, the input side of the search

**Done — one commit on `phase-173-hypothesis-gap`, phase complete.** Phase 172's "Next" said the honest question may be what the 383 quiescent runs *are* missing rather than which rule to add. This is the first half of that, and it found a real defect.

- **The question nobody had asked: is the prover told what the problem says?** `hypotheses()` reads parent ties off a `Construction`; Newclid's `JGEXDefinition.clauses` states, per macro, the predicates that macro asserts. Two independent routes to the same set, so their diff is a measurement rather than an opinion. Extracted the 89 definitions statically (no Newclid runtime — its deps are not installed here) and compared against the seeded set on all 466 built problems.
- **4 739 predicates stated: 4 069 seeded, 549 not seeded but derived anyway, 0 refused by the screen, 121 absent at the fixpoint on 45 problems** — 41 of those quiescent and unproved. So the input was 97.4% right, and the 2.6% was concentrated: `incenter2` 64, `parallelogram` 56, `circle` 1.
- **Two real gaps, both in `hypotheses()`, both facts the construction genuinely guarantees** — which is that function's stated contract, so a defect and not a missing feature. A `TranslatedPoint` emitted one pair of a parallelogram's sides and not the other (`t − point == vectorTo − vectorFrom` rearranges to `vectorFrom − point == vectorTo − t`). An `Incenter` said nothing about its touch points, though it is equidistant from all three sides and hence from the feet of its perpendiculars — the incircle, stated without being drawn, read cross-object the way `ReflectedPoint` already reads its mirror. The `circle` 1 is a probe artifact: `Circumcenter` emits a *spanning* pair of radii and leaves the third to follow.
- **Corpus: 64 → 64 proved, and the same 64 problems** — diffed by name from two `--verbose` runs, not inferred from the count. Cost: two `incenter2` problems (4 goals) move *unproved → undecided*; neither state is a proof and none was lost.
- **Kept, and deliberately not on the `para_coll`/R50/R21 disposal rule.** A rule is optional machinery dropped when it has no consumer; a hypothesis is what the document *says*. A prover not told its own figure's parallelogram is wrong independently of what a benchmark scores, and the same hole was there for any user drawing either configuration in the app.
- **The gap re-measured after the fix: 121 → 1.** The input side is closed as an explanation.

Gates: analyze clean, suite **3405** (3402 + 3), fixture suite unchanged.

**Next — a much narrower question than it was this morning.** Both cheap explanations for the 379 quiescent-unproved runs are now closed: not the rule table (four levers, three zero, R01 and R30 worth 18 goals between them), not the hypotheses (4 738 of 4 739 present). What is left is that the deductive closure of the stated hypotheses under this table genuinely does not contain those goals — which points at **auxiliary construction**, the one piece of machinery this repo has built (Phase 153) and never run at corpus scale. `imo_102_requires_aux.txt` is 44 built / 0 proved and is literally its rig. The blocker is the cost model, not the search: Phase 153 measured one candidate as costing a *full re-run*, so an exhaustive sweep is (candidates) × (baseline), 50 s on a single document. Budget before code. Carried: `readFact` on listed `eqangle`s can still read magnitude-false; Phase 161's decision box, which the two newly-undecided problems now have a second claim on.

**Gotchas.**

- **`git checkout <file>` mid-session is the cheapest before/after rig there is**, and it is how the "same 64" claim was earned: run the bench verbose with the change, restore the one file from the index, run it again, `comm` the two proved lists. Counts that match are not the same set, and Phase 171 lost a problem a proof in exactly that blind spot.
- **A hypothesis and a rule have different disposal rules**, and conflating them would have deleted this phase's work: the corpus number is flat, so the `para_coll` precedent says drop. It does not apply — that precedent is about machinery with no consumer, not about the prover being told its own figure. Worth having the distinction written down before the next flat measurement.
- **Newclid's Python does not import here** (no `matplotlib`), so anything needing its definitions has to parse them. They are regular enough for a static read — `JGEXDefinition(...)` blocks with a `clauses=JGEXClause.from_str("…")` — but bound the block per top-level assignment, or a definition without `clauses` silently borrows the next one's (`triangle` came back with `perp a b a c` on the first attempt).
- The probes for both this phase and Phase 172 ran the whole corpus at **230–450 s a pass**, and this phase needed five passes. Budget for that before starting; a classification pass that skips the prover re-run is much cheaper when the question is diagnostic.

## 2026-08-28 — session 179 (continued): Phase 174, the ceiling for auxiliary construction

**Done — one commit on `phase-174-aux-ceiling`, phase closed as a measured negative.** Phase 173 named auxiliary construction as the last surviving explanation for the 379 quiescent-unproved runs. This asks the prior question before costing Phase 153's search: **would a perfect one help?**

- **The corpus keeps an answer key and Phase 167 refuses to open it.** Auxiliary points are stated after a `|`; that refusal is right for a benchmark (building them would measure the corpus's authors) and wrong for exactly this question, so `withAuxiliary` was added to `translateNewclidProblem`, off by default and pinned.
- **Correcting Phase 173's own "Next" before anything else: `imo_102_requires_aux.txt` states no auxiliary sections at all.** Its 44 problems need auxiliary points; the file does not say which. It is not the rig that entry called it. Corpus-wide only **17** problems state one — 16 in `imo.txt`, 1 in `imo_sl.txt`.
- **0 of 17, and 0 of the 8 that build.** Nine fail translation identically in both arms (4 `unknownMacro`, 3 `unsupportedGoal`, 1 `degenerate`). All eight of the rest: `unproved` before, `unproved` after.
- **The mechanism fires, which is what makes the zero mean something** — Phase 170's correction, applied a third time. Every one of the eight gains facts: `imo_sl_2015_g5_constr` **139 → 354**, `2019_p6` 119 → 180, `2009_p2` 38 → 70, smallest gain 12. The answer key adds up to 2.5× the fixpoint and closes nothing.
- **And it is not budget.** At 10× (300 000 applications) all eight reach *quiescence*, including `2008_p1a`, the only one undecided at the provider's own budget. A quiescent unproved run has exhausted its closure.
- **So the upper bound is zero.** A search that has to *find* the points is strictly weaker than being handed them, so whatever Phase 153's machinery scores on these eight is ≤ 0, and the cost model is not worth solving for this.

Gates: analyze clean, suite **3406** (3405 + 1).

**Next.** Three explanations survive for the 379, and one measurement separates all three: **run Newclid over the same corpus and diff the proved sets.** If the reference implementation also fails on these, this prover is at parity and the limit is DDAR's method — which would leave constants (`aconst` 12 goals, `rconst` 9, `lconst` 6) as the only remaining lever worth its cost. If Newclid proves a large share, there is a concrete target list and the gap is in this implementation. **That run needs a Python environment Newclid can import** — its deps are not installed in the checkout (`import newclid` dies on `matplotlib`) — so it is an install decision for the user to make before it is a measurement. Carried: `readFact` on listed `eqangle`s can still read magnitude-false; Phase 161's decision box.

**Gotchas.**

- **A negative measurement is only worth the check that the mechanism fired**, and this is the third phase in a row where that check carried the finding (R21, Phase 172's split, this). Verdicts identical in both arms looked at first like a flag that had not taken effect; printing object / named-point / hypothesis counts per arm proved it had, and then the fact counts turned "nothing moved" into "up to 2.5× the content, and nothing moved".
- **A file named `*_requires_aux` does not contain its aux.** Two entries of mine called it the rig for Phase 153's search on the strength of the name. Check the `|` count before repeating that.
- **Raising the budget 10× is a cheap way to separate "closure exhausted" from "ran out of road"**, and it is worth doing before reporting any unproved verdict as a limit of the rule table. It cost one extra run here and converted the one `undecided` in the sample into a quiescent `unproved`, which is what let the ceiling claim be stated without a caveat.

## 2026-08-28 — session 179 (continued): Phase 175, the comparator that could not be run

**Done — one commit on `phase-175-comparator`, phase closed as "could not be measured".** Phase 174 named running Newclid over the same corpus as the measurement that separates what is left. The environment and harness are built; the comparison is not available, and why is the finding.

- **The environment, at the user's request.** Newclid's deps are not installed in the checkout, and its `pyproject.toml` wants Python ≥ 3.11 while this machine's default is 3.14 — too new for the `scipy`/`symengine`/`matplotlib` wheels it pins. A **3.12 venv at `~/Code/var/Newclid/.venv`** (already gitignored in that repo) plus `pip install ./newclid` works. **Nothing in that checkout was modified.**
- **The harness** is `benchmark/comparator/run_newclid.py` with a README covering setup, invocation and every patch. One goal per problem, matching regula2's parser; auxiliary sections not built, matching both defaults; each problem in a **forked child with a hard timeout**. `corpus_bench.dart`'s verbose lines are now source-qualified — problem names repeat across files, so a bare name cannot be diffed against anything.
- **Newclid 3.0.1 — the latest release, and the checkout's own version — does not run its own corpus.** Five crash classes. **Three are unambiguous bugs**, fixed in the venv's installed copy with the fix the authors plainly intended: `expected_lconsts.copy()` iterated as pairs when it is a `dict` read with `.items()` everywhere else (a `# pyright: ignore` hid it); `Diff` built as a hardcoded pair though `Diff.points` is already `tuple[Point, ...]` with a pairwise check and their own R34–R37 pass `diff A B C`; and a rule conclusion's "variables" including literals like R51's `1/2`, looked up in a mapping of point variables. **Two are not one-liners and were left alone** — `NotImplementedError: LengthEquationPredicate is not implemented` and a `ValidationError` on the degenerate `simtri c b d c b d` its own matcher generates — because repairing those means rewriting the reference.
- **The rate, on a seeded 150-key random sample of the 466 built: 146 goals ran, 16 gave a verdict — 11%.** 117 errors (55 `NotImplementedError`, 50 `ValidationError`, 10 `TypeError`, 2 `ValueError`) and 13 timeouts at 30 s. The two dominant classes are **exactly the machinery the comparison was for**, so the surviving 11% is biased toward what Newclid finds easy.
- **What the 16 say, with its n stated.** 6 proved by both, 2 unproved by both, 1 proved only here (`examples.txt:tangents_to_circle`), 7 only by Newclid — and **six of those seven are Newclid's own rule-unit problems**, five in `testing_minimal_rules.txt` plus `rule_r29_not_found_explicit`. That is the exclusion Phase 171 already made for that file, and `r46` is literally the same problem it excluded then. **One genuine benchmark problem each way.** Rough parity on the survivors, and no evidence the reference reaches goals this prover cannot.

Gates: analyze clean, suite unchanged at **3406**.

**Next.** The decisive measurement is off the table short of repairing someone else's prover, so the benchmark's ordering resumes at its last untried item: **constants** — `aconst` 12 corpus goals, `rconst` 9, `lconst` 6 — measured before built, like the four levers before it. Worth holding against it that 27 goals is a small prize and that `cong` (259) and `coll` (131) are where the mass is. Carried: `readFact` on listed `eqangle`s can still read magnitude-false; Phase 161's decision box.

**Gotchas.**

- **An in-process `SIGALRM` does not bound a library that blocks in C.** A 60 s alarm let one problem run 342 s. Fork a child and `terminate()` it; the fork inherits the imports, so the restart is free.
- **An alphabetical prefix is not a sample**, and the first partial run's 88% error rate was measured on one — `imo.txt` sorts early and is the hardest file. The seeded random sample is what the 11% figure rests on. Do not quote a rate off a prefix.
- **Patching the reference to make it run is legitimate only while the fix is the one its authors plainly intended**, and the line is worth stating: three of these were type declarations and sibling call sites agreeing against a single wrong line; the other two would have been me writing new reasoning and calling the result "the reference". Everything patched is tabulated in the README.
- **Patch the venv's installed copy, never the checkout.** `pip install ./newclid` copies the source, so the two are separable and the user's repository stays clean.
- **`testing_minimal_rules.txt` is a per-rule unit suite, not a benchmark**, and it has now inflated a result twice — Phase 171 caught it and this phase caught it again on the same rule, `r46`. Any future comparison should exclude that file up front.

## 2026-08-28 — session 179 (end): the comparison, kept re-runnable

**Session close.** Four phases landed and merged today — 172 (the direct/reflected split, measured and dropped), 173 (121 stated hypotheses the runs never had, two `hypotheses()` gaps closed), 174 (the auxiliary ceiling, zero), 175 (the comparator, set up and found unusable). This last commit is the notes, because the comparison is the one thing left unfinished and the user's standing goal is that **regula2 should not be out-proved by Newclid or any comparable reference prover on a shared corpus** — now recorded in PLAN, since it decides whether an unanswerable question gets closed or kept open. It is kept open.

- **`benchmark/comparator/patch_newclid.py`** — the three unambiguous fixes, idempotent, reporting each as applied / already-present / **NOT FOUND** (which on a fresh install means the line moved or upstream fixed it). Round-trip tested: reverted one fix, re-ran, it re-applied and Newclid still solves. Without this the repairs lived only in a venv that a `rm -rf` would erase.
- **The README now opens with a copy-paste re-run recipe** — venv, version check, patcher, regula2's built set, the runner — plus the two traps already paid for (an alphabetical prefix is not a sample; exclude `testing_minimal_rules.txt`) and the bias direction.
- **Phase 176 is open in TODO**, blocked on upstream rather than on us. The trigger to watch is a Newclid past 3.0.1 or either unpatched crash class being fixed; those two are **105 of the 117 sampled errors**, so they are most of the 89% loss. Repairing them ourselves would mean writing new reasoning into someone else's prover, which is where Phase 175 drew the line and where it should stay.
- **Memory updated**: `reference-provers-and-corpus` now covers the venv and the harness, and `newclid-cannot-run-its-own-corpus` records the block, the trigger and the two traps.

Gates: analyze clean, suite **3406**.

**Next session.** Either the comparator (if the trigger has fired — check `pip index versions newclid` first, it is one command) or constants, the benchmark's last untried lever: `aconst` 12 corpus goals, `rconst` 9, `lconst` 6. Measure before building, as with the four levers before it. Standing caveat on constants: 27 goals is a small prize against `cong`'s 259 and `coll`'s 131, so it is worth asking whether the corpus is still the right thing to optimise against before spending a phase on it.

## 2026-08-31 — session 180: Phase 177, constants measured — three were respellings

**Done — one commit on `phase-177-constants`, phase closed.** The benchmark's last untried lever, measured before built like the four before it. Trigger for Phase 176 checked first: `pip index versions newclid` still says 3.0.1, so the comparison stays blocked and the ordering resumed at constants.

- **31 constant goals, not the 27 prior entries remembered** — `aconst` 16, `rconst` 9, `lconst` 6. Classified by running the real translator on each: **9 sole-blockers** (figure builds, goal refused; 8 outside `testing_minimal_rules.txt`) against **22 gated by unsupported constant-stating macros** (`s_angle` 13, `lconst` 6, …). All six `lconst` goals are in the gated set. Supply side: those macros alone gate **44 of 928 problems**, 23 with goals already statable — constants in this corpus are mostly something the figure *states*, which is Phase 173's theme again, and Newclid agrees: its own rule table touches constants in exactly one rule (R51).
- **Three of the eight real sole-blockers respell as the existing vocabulary** — `aconst … 1pi/2` *is* `perp`, `rconst … 1/1` *is* `cong`, biconditionally — and all three proved on the spot. Built as `_respelled` in `newclid_translation.dart` (+4 tests): translation fidelity, not machinery, on Phase 173's hypothesis-versus-rule distinction — refusing a goal the vocabulary states misreports the corpus. **Corpus 466 → 469 built, 64 → 67 proved**, the three named problems exactly (`two_goals_cong_aconst#2`, `regular_square#2`, `geometric_ratios`), 0 unsound, unproved/undecided unchanged at 379/23.
- **The genuine remainder, measured at the fixpoint and declined.** The two eq_triangle `aconst` goals are pinned by the angle closure *up to a threefold root choice* — `3·(θ_cd − θ_ab) ≡ 3k` is entailed where the unscaled equation is not, mod-1 having no division by 3 — so an `aconst` build is a value-carrying predicate plus a root-screening reader, for **+2**. The two real `rconst` goals need the length system's missing constant term (Phase 165's `ln 2` note): **+2**. `regular_hexagon#2` is unreachable for non-constant reasons. The macros would unlock 44 problems at unknown yield and need the vocabulary first. None of that is the next phase against `cong` 259 / `coll` 131.
- Gates: analyze clean, suite **3410** (3406 + 4).

**Next.** The benchmark's ordering is exhausted: every lever it named is measured. Standing answers — the rule table is right (four zeros, R01 +16, R30 +2), the inputs are right (Phase 173), auxiliary points would not help (Phase 174), the reference cannot be run (Phase 175, trigger still cold). The 379 quiescent-unproved are beyond this DD+AR closure's *reach*, not its inputs, so the honest next questions are outside the corpus loop: either the app-facing track (the prover exists to serve documents, and "what is this angle?" — the aconst reader — has user value beyond its +2), or accept the corpus number as the plateau and return to the kernel/UI phases. Carried: `readFact` on listed `eqangle`s can still read magnitude-false; Phase 161's decision box.

**Gotchas.**

- **A goal outside the vocabulary is not always a missing feature — check its stated value first.** Three "constant" goals were `perp` and `cong` wearing Newclid's spelling, provable all along. The corpus states `aconst`/`rconst` at whatever value the figure has, including values that collapse into plainer predicates.
- **The angle closure holds more than `entails` admits at face value: scale the query.** `x ≡ k` failing while `3x ≡ 3k` holds is the signature of a root ambiguity, not absence — mod-1 linear algebra cannot divide. Asking only the unscaled equation would have read "pinned up to finitely many candidates" as "unreachable".
- **`translateNewclidProblem` orders its refusals: `unknownMacro` fires before `unsupportedGoal`, and `unsupportedGoal` fires only after the whole figure built.** That makes the translator itself the honest classifier for "what is the sole blocker" — no static macro-list needed for the goal-side question (the probe kept one only for the corpus-wide supply counts).

## 2026-08-31 — session 180 (continued): Phase 178, budget was not a lever either

**Done — one commit on `phase-178-undecided-budget`, phase closed as a measured negative that completes a claim.** The session report named the 23 undecided as the one pocket where "more of the same" was untested — they exhausted the 30 000-application budget rather than their closure, so no reach claim covered them. One throwaway probe (Phase 168 pattern, deleted): the exact 23 source-qualified rows of the Phase 177 bench run, re-run at 300 000 applications, same chunking, same stop-on-proved.

- **0 proved, 23 quiescent-unproved, 0 still undecided.** No corpus number moves; the headline at the provider's own budget stays 469 / 67 / 379 / 23.
- **20 of 23 quiesce within 33–41 k applications** — they were just over the line, not mid-explosion. Only two shapes needed real headroom: the one jgex problem (210 k) and `1995_p1` (133 k). Duplicates across corpus files land on bitwise-identical apps/facts counts.
- **The claim this completes: every buildable unproved corpus problem — 402 of 402 — exhausts its deductive closure under this table.** Budget is not a lever on this corpus, full stop. That also answers the corpus-side arm of Phase 161's decision box: application budgets are a latency/fairness question, not a proving-power one.

Gates: analyze clean; no `lib/`/`test/` change, suite stands at **3410** from this session's Phase 177 run.

**Next.** With budget closed, the remaining prover options are exactly the session report's list: (1) the Phase 176 trigger check each session (`pip index versions newclid` — still 3.0.1 today); (2) the constants stack as a deliberate decision — the only dismissed lever whose *combined* yield (44 macro-gated problems) is unmeasured, and the largest build; (3) the app-facing `aconst` reader, whose value is the user question "what is this angle?" rather than its +2; (4) otherwise the corpus number is a plateau and the kernel/UI track resumes. Carried: `readFact` on listed `eqangle`s can still read magnitude-false; Phase 161's decision box (now minus its corpus arm).

**Gotchas.**

- **An "undecided" bucket can be almost entirely edge-of-budget**: 87% of these quiesced within 37% more applications. Before treating undecided counts as a search-depth signal, check how far past the budget they actually needed to go.

## 2026-08-31 — session 180 (continued): Phase 179, the rule that was a negative and the bug that was not

**Done — one commit on `phase-179-r49-congonly`, phase closed at +2 proved, neither of them from the rule it set out to measure.** The candidate was R49 cong-only (`cong(o,a,o,b) & cong(o,b,o,c) & cyclic(a,b,c,d) => cong(o,a,o,d)`), the one rule-shaped lever the fixpoint-composition argument did not cover — Phase 169 dismissed it as subsumed by R50's negative instead of measuring it.

- **The measurement flagged an UNSOUND proof, and that flag was itself wrong — which is the finding.** `regular_hexagon`'s proof carried an angle step whose recorded premises "did not entail" its conclusion. The certificate was in fact valid (recombine == query; it scales one perp by 4 and another by 2, wiping their ½s mod 1). The false refusal came from `AngleClosure.add`'s Bézout branch: it continued elimination with a determinant-|y| combination of the two merged rows — **non-unimodular, so the row basis silently generated a sublattice of the inputs**. Consequences ran both directions: the verifier could report sound steps unsound, and the live engine could miss entailed facts. Pre-existing since the closure was written; R49's only real contribution was producing the first proof whose verification walked the lossy path.
- **Fix: the classic unimodular complement** (`(theirs/g)·current − (mine/g)·old`, determinant −1), with a regression test distilled to the exact seven equations in citation order — failing before, passing after. **The fix alone: 67 → 69 proved** (`regular_hexagon`, which Phase 177 had measured as out of reach, and a JGEX book problem), no losses, 0 unsound, no pinned fixture moved.
- **R49 itself, measured on the repaired engine: R50's trade restated** — +1 genuine (`point_on_circle_eqdistant_from_center`, the rule's own statement as a problem) for +3 undecided, the same `cyclic`-premise join cost. Reverted on the R50 protocol; recorded in the rule table's doc comment.

Gates: analyze clean, suite **3411** (3410 + 1). Corpus baseline: **469 / 69 / 377 / 23 / 0 unsound**.

**Next.** The rule-side ledger is now fully settled: every Newclid rule candidate is measured, kept, or recorded-and-declined. What remains is the session report's decision list — the constants stack (largest build, unmeasured joint yield over 44 macro-gated problems), the app-facing `aconst` reader, the Phase 176 trigger, or the plateau. One new thread the bug opens: **the closure was under-proving for its whole life, and Phase 172/174-style "measured at the fixpoint" zeros that consulted `entails` are worth a skeptical re-glance** — the published `para`/`perp` conclusions went through the same lossy basis. Phase 172's split and Phase 174's ceiling used fact-set/goal checks, not raw `entails`, so their zeros likely stand; Phase 177's threefold-root finding was re-confirmed against the *fixed* closure by the regular_hexagon flip only in part. Cheap to re-ask if anything ever hinges on them.

**Gotchas.**

- **An UNSOUND flag can indict the checker rather than the proof.** Verify the certificate (`recombine == query`) before believing either side — here the proof was right and the verifier's fresh rebuild was the broken party, and the asymmetry was pure luck of insertion order.
- **Integer-lattice row reduction must be unimodular at every step**, and the Bézout shortcut that stores the gcd row and reduces with it is the textbook way to lose that: the determinant of (replacement, current−k·replacement) against (current, old) is −y, not ±1. If a closure ever gains another elimination branch, check the determinant first.
- **A rule can look like it proves a problem when it is actually routing around a bug.** R49 "proved" regular_hexagon on the broken engine; the fixed engine proves it with no new rule. Without separating the two changes into two bench runs, R49 would have been credited +2 and kept.

## 2026-08-31 — session 180 (end): Phase 180, the constants stack designed

**Session close — five phases landed today** (177 constants measured, 178 budget negative, 179 the lattice fix + R49 reverted, 180 this design; plus the session-opening trigger check). Corpus stands at **469 / 69 / 377 / 23 / 0 unsound**, suite **3411**, all merged and green on `main`.

**Done — one commit on `phase-180-constants-design`, docs only.** The user chose the constants stack over the aconst-reader, plateau and JGEX-comparator options. PLAN §"The constants stack — vocabulary as payload, algebra as reader" records the design; TODO opens Phase 181.

- **The three decisions**: constants are value-carrying fact kinds (`aconst`/`rconst`/`lconst`) that **no DD rule matches** — hypotheses state them, AR absorbs them, asks answer them (Newclid's own table touches constants once, and that lands as `Midpoint` emission, not a rule). The **ℤ-module line holds**: no root-screening reader; an `aconst` goal proves on unscaled entailment or stays a named incompleteness. The length system's constant column is a **formal ℚ-vector over {ln p}** (a plain rational breaks under ℚ-row-scaling), making the system inconsistency-capable as Phase 165 predicted.
- **The supply theorem sets the phasing**: every present hypothesis constant is 0 or ½, and ℤ-combinations of halves are halves — so an `aconst` ask before the constant-stating macros has zero reachable corpus targets. Phase 181 = length column (immediate prize: 2–3 goals in already-building problems); Phase 182 = the seven macro objects + `aconst` together, whose measurement over the 44 gated problems is the phase's product; Phase 183 = that number's verdict.
- **Macros are real construction objects** whose `hypotheses` emission states the constant they embody — the benchmark section's `on_aline` ruling applied. New codec kinds: check how v1 readers fail on unknown kinds before shipping (version-field rule).

**Next session: Phase 181** — its checklist is in TODO. Carried: `readFact` on listed `eqangle`s can still read magnitude-false; Phase 161's decision box (minus its corpus arm, closed by 178); the skeptical re-glance STATUS 179 noted for old `entails`-based zeros, should anything come to hinge on them.

## 2026-09-01 — session 181: Phase 181, the length constant column — +4 proved, and the branch was never sampled

**Done — six commits on `phase-181-length-constants`, phase closed.** The constants stack's first implementation phase, built in checklist order and measured last. Corpus baseline **469 / 69 / 377 / 23 / 0 → 483 / 73 / 387 / 23 / 0**, suite **3446** (3411 + 35). Phase 176 trigger checked at session start: `pip index versions newclid` still says 3.0.1, comparison stays blocked.

- **The column is a formal ℚ-vector over `{ln p}`, exactly as designed** — `Map<BigInt, Rational>`, prime → exponent, so scaling by ½ turns `ln 2` into `{2: ½}` with no rounding anywhere. Unique factorization makes the vectors ℚ-independent, so an empty-variable row with a surviving constant is a genuine `0 = k`: `isContradictory`, `LengthAddOutcome.contradiction`, and the latched `isInconsistent` the angle side has always had. Entailment stays conservative under inconsistency (no ex falso), mirroring `AngleClosure`. The evaluator sweep — this file's signature property — extends componentwise: a formal assignment is a rational part plus `ln 2`/`ln 3` parts, and span-equals-annihilator survives the constant because the constant is linear in the coefficients.
- **`rconst`/`lconst` are the first value-carrying fact kinds, and the value is identity.** The one genuinely new canonicalization problem: `rconst`'s pair swap inverts its ratio, so the symmetry is value-coupled — `Fact` canonicalizes it with the value (lesser value breaks a same-segment tie), and `orbitArguments` honestly lists only the value-preserving forms. `lconst` is pinned as the vocabulary's one non-scale-invariant statement. Adding two enum members forced every exhaustive switch to declare itself, which is how all fourteen consumers were found; the builder's "every kind has a template" invariant is now "every point-pure kind", with the reason (a value needs an input surface a slot list does not have) stated in the test.
- **The emission is three lines and it buys a rule.** `Midpoint`, exact-half `SegmentRatioPoint` and `CentralReflectionPoint` emit `rconst(p₁,m,p₁,p₂; ½)` beside their `midp`; one half suffices, the closure derives the other from the cong. Named targets `midpoint_splits_in_two` and `r51` prove immediately — and so does **`r29` in both corpus spellings**: Newclid's R29 (midpoint-is-an-eqratio) is exactly two rconst-½ rows meeting in the closure. A DD rule bought by AR.
- **`obm_1fase_2006_p15` was the target that paid differently: it flushed a translator defect.** Its goal (`|bd| = 2|fg|`, the classic SAS-congruence on two erected equilateral triangles) holds exactly when the two `eqdistance` apexes take *consistent* orientations — and `_cross`'s "branch 0 is canonical, either answer is a correct instance" made the goal-true instance unreachable at any number of samples, while Newclid's own semantics resample until the goals hold. The branch an unpinned crossing takes is now drawn with the attempt's generator, part of the sample like the positions. **`goalFalseInFigure` dropped 11 → 0** — every one of those eleven skips was a fixed-branch artifact, and all build now (obm included, quiescing unproved at 13 k apps: its congruence chain wants the 60° angles, which is Phase 182's `aconst`).
- **The branch change cost nothing on the proved side and two marginal figures on the built side.** The proved set is bitwise identical with and without it (measured as two separate runs, Phase 179's two-bench discipline). `jgex E061-62` and `usamo 2005_p3` flap to `unstableFigure` because branch draws shift the shared sample stream — both were unproved before, both were near-degenerate samples all along ("came apart" under perturbation).

Gates: analyze clean, suite 3446, fixtures untouched, 0 unsound, full name diff both ways.

**Next session: Phase 182** — the seven constant-stating macros as construction objects plus the `aconst` kind and ask, landed together per the supply theorem; checklist opened in TODO. Its measurement over the 44 macro-gated problems is the phase's product. Carried: `readFact` on listed `eqangle`s can still read magnitude-false; Phase 161's decision box (minus its corpus arm); the skeptical re-glance on old `entails`-based zeros.

**Gotchas.**

- **A "canonical" branch is a claim about problem semantics, not a convenience** — "either answer is a correct instance" was true for `eq_triangle` alone and false the moment a goal distinguished branches. If a figure sampler ever again fixes a choice per policy rather than per sample, check whether anything downstream conditions on it.
- **An exhaustive enum switch is the cheapest consumer census there is.** Adding the two kinds without defaults surfaced every site that had to decide (fourteen), including two `Fact(fact.kind, points)` rebuilds in test/bench code that would have silently dropped the value if `Fact`'s constructor did not enforce presence. Value-presence-as-ArgumentError caught both at first run.
- **The corpus's by-goal table keys the original goal predicate**, so a respelled `rconst … 1/1` still counts under `rconst`. Read the name diff, not the table, when attributing gains.
- **Branch draws share the position sampler's RNG stream**, so any problem with an unpinned multi-branch crossing samples different positions than the previous baseline even at the same seed. Expect one or two marginal figures to flap on any future change to what consumes that stream; the name diff is what separates a flap from a regression.

## 2026-09-01 — session 182: Phase 182, the macros and aconst landed together — +42 built, +19 proved

**Done — seven commits on `phase-182-const-macros`, phase closed.** The constants stack's second implementation phase, built supply-with-demand per the supply theorem and measured last. Corpus baseline **483 / 73 / 387 / 23 / 0 → 525 / 92 / 409 / 24 / 0**, suite **3489** (3446 + 43). Phase 176 trigger checked at session start: `pip index versions newclid` still 3.0.1.

- **`aconst` cost almost nothing beyond its canonicalization.** The kind is `rconst`'s value-coupled pattern with negation-mod-1 where `rconst` inverts (`_canonicalRconst` generalized to `_canonicalValueCoupled`; 0 and ½ are the negation's fixed points); absorption is one `AngleEquation` row whose constant is the stated value; and the ask needed **zero prover changes** — `resolve()` already routes any fact through `entailmentOf`, so teaching `equationOf` the kind was the whole hookup. The half *is* `perp` inside the algebra (pinned as a test), which is the `_respelled` biconditional held where it belongs. Convention checked against Newclid's own `constant_angle.py`: `aconst(a,b,c,d; r)` is `θ_cd − θ_ab ≡ r·π mod π`.
- **`Rational` moved to `lib/domain/math/`** before the objects were written: the carriers hold exact stated values, and construction must not import the prover built on top of it. The file has zero imports; pure move.
- **Four carrier kinds cover all seven macros** — `FixedAngleLine` (s_angle + the aconst macro), `StatedRadiusCircle` (lconst/l2const), `ScaledCompassCircle` (rconst, triangle12), `RatioApolloniusCircle` (rconst2). Euclidean-only like the vocabulary they feed. Codec params are canonical `n/d` strings, never JSON numbers — thirds in the round-trip corpus so a codec that rounded would fail. Additive kinds, no version bump (the `FivePointConic` precedent: a v1 reader refuses an unknown type loudly). Painter/hit-tester needed no arms — they read the sealed projections.
- **Value-1 collapses route to plainer carriers in the translator**: `rconst 1/1` builds the compass circle, `rconst2 1/1` the perpendicular bisector, sharing the existing dedup keys, so `cong` stays the statement where `cong` is the statement. `l2const` takes an exact rational square root and refuses `√2`.
- **The measurement: 45 problems left `unknownMacro` (209 → 164), 42 built, +19 proved, 0 lost, 0 unsound, unstable set identical.** The 19 include `worlds_hardest_easy_geometry_problem1_with_construction` and `1995_p1#2`, both proved at **0 applications** — pure AR entailment of the stated angles, no DD needed. `obm_1fase_2006_p15` stays quiescent-unproved as Phase 181 predicted: its 60° lives in an `eq_triangle`, which states congs, and the threefold root stays refused (ℤ-module line held).
- **The 3 `goalFalseInFigure` are corpus defects, not translator ones — verified against the reference's own arithmetic.** `obm_1fase_2001_p2`/`p9`/`2008_p16` state `aconst` goals false under Newclid's own `angle_between_4_points`: `p16`'s figure is branch-free and fully determined mod π, its own sketches (`rotatea` is counterclockwise, like ours) put the goal at 130° against a stated 50° — a chirality transcription error, since the mirror figure satisfies it. Newclid's own builder could never satisfy these numerically either.

Gates: analyze clean, suite 3489, fixtures untouched, full name diff both ways.

**Next session: Phase 183** — the verdict on the number, a decision with the user: app-facing constants (tools + the "what is this angle?" reader for the four new carrier kinds — the prover can now state a fixed angle but no user can draw one), plateau at 92/525 and back to the kernel/UI track, or another prover thread. Carried: `readFact` on listed `eqangle`s can still read magnitude-false; the skeptical re-glance on old `entails`-based zeros; Phase 176 trigger.

**Gotchas.**

- **A goal false in every sample is worth checking against the reference's own arithmetic before blaming the translator.** Hand-computing Newclid's `angle_between_4_points` on a branch-free figure settled in ten lines what could have been a day of hunting a nonexistent sign bug — and the answer ("the corpus is wrong, deterministically") is a different phase-183 fact than "we read s_angle backwards".
- **A directed-angle convention cancels out of consistency**: mirroring a figure negates every stated turn *and* every goal mod 1, so a problem consistent under one chirality convention is consistent under the other — which is exactly why a *mixed* transcription (figure clauses one way, goal the other) is unsatisfiable at any sample count and shows up as `goalFalseInFigure`, never as flakiness.
- **Growing an enum can move a quiescence boundary.** `regular_hexagon#2` drifted unproved → undecided with no behavioural change relevant to it — application scheduling shifted and it now crosses the 30 000 line before quiescing. Phase 178's claim ("every buildable unproved problem exhausts its closure") now carries that one asterisk until anything hinges on it.
- **`FixedAngleLine` under the generic `RelativeLine` coincidence case would have merged different turns into one carrier** — a value-carrying line kind needs its own `coincidentCarriers` arm *before* the generic one, and the turn is part of the identity. Any future value-carrying `RelativeLine` subclass has the same trap waiting.

## 2026-09-02 — session 183: Phases 183–184, the verdict and the constant tools — the app draws what the prover reads

**Done — Phase 183 closed on the user's verdict, Phase 184 built and closed, five commits on `phase-184-constant-tools`, merged to `main`.** Suite **3489 → 3517** (+28), analyze clean, corpus and fixtures untouched (no prover change). Phase 176 trigger checked: `pip index versions newclid` still 3.0.1.

- **The verdict: app-facing constants first.** Presented with the three candidates (app-facing, plateau-and-kernel, another prover thread), the user chose the app-facing thread, on the asymmetry orientation turned up: the app's own constant tools (`FixedRadiusCircle`, `RotatedPoint`) carry floats and emit no hypothesis, while the four exact carriers emit `aconst`/`lconst`/`rconst` and no user could draw one. Recorded in PLAN §"The constants stack's verdict"; split into Phase 184 (draw) and Phase 185 (ask), because an ask is cheaper to design once its objects can be drawn.
- **The two families stay two** — the design decision the phase had to make. The float tools are not converted to rational-when-possible: that would make `hypotheses()` depend on how a number was typed (`2.5` states, `sqrt(2)` does not), a silent change of meaning on a keystroke. The exact tools sit beside their float twins in the same flyouts, and the difference is spelled where the user chooses.
- **`Rational.tryParse`** is a parser, not a rounding: integers, decimals (exact over a power of ten), integer fractions; `sqrt(2)`, `pi`, `1/0` are null — the dialogs' garbage-is-cancel convention with irrationals deliberately on the garbage side. Pinned with a 34-digit decimal a double could not hold.
- **Four tools, one shape each.** `FixedAngleLineTool` subclasses `PointAndLineTool` and refuses non-Euclidean input in `onInput` (the base class has no `availableUnder`); the three circle tools are `MultiPointTool`s. Value contracts are checked when the tool is picked, not on the tap. Value-1 collapses mirror the translator: compass scale 1 → `CompassCircle`, Apollonius ratio 1 → `PerpendicularBisectorLine`; a stated angle of 0 or ½ stays a `FixedAngleLine`, as in the translator.
- **Dialogs, chords, rows, labels.** One `_RationalDialog` behind four asks; the angle is asked in degrees and stored as `(deg/180).modOne()`, so −120 and 60 are one line. `G ⇧ D` / `G ⇧ R` / `G ⇧ O` / `G ⇧ Q`. `euclideanOnlyConstants` beside `euclideanOnlyMacros`, passed to the Lines and Circles groups, cross-checked against the tools by the toolbar test. Kind labels spell the exact value: `Line at 45/2°`, `Circle of radius 5/2`, `Compass circle × 3/2`, `Apollonius circle, ratio 2`.

**Next session: Phase 185** — the ask: value-carrying templates (`aconst`/`rconst`/`lconst` with a value field in the builder bar), the closure reader (`entails` returning the residual constant instead of checking it is zero; ℤ-module line held, no root screening), and wiring the reader as a slot fill so the answer arrives with a proof. Checklist in TODO. Carried: `readFact` on listed `eqangle`s can still read magnitude-false; the skeptical re-glance on old `entails`-based zeros; Phase 176 trigger; `regular_hexagon#2`'s quiescence asterisk.

**Gotchas.**

- **A `PointAndLineTool` subclass cannot use `availableUnder`** — that hook is `MultiPointTool`'s. Overriding `onInput` to check `input.absolute` first is the equivalent, and the toolbar cross-check test has to probe it through `onInput` rather than through the hook.
- **`MultiPointTool` refuses an already-collected point**, so a test that wants the centre to be a span point cannot have it — the first draft of the compass test tried exactly that and read the `ToolIgnored` as a bug.
- **Import placement in `object_kind_label.dart`**: `domain/math/…` sorts after every `domain/construction/objects/…`, and the `directives_ordering` lint is the only thing that says so. Cheap to get wrong when inserting by neighbour.
- **The flyout row a test taps is the name half of the label** — `find.text('Circle with stated radius…')`, not the full "(tap the center)" string; `ToolMenuRow` splits the parenthetical into a subtitle and keeps the ellipsis on the name line.

## 2026-09-02 — session 183 (continued): Phase 185, the ask and the reader — "what is this angle?" answered with a proof

**Done — three commits on `phase-185-constants-ask`, phase closed, merged to `main`.** Suite **3517 → 3541 (+24)**, analyze clean, no prover behaviour change (readers only, corpus untouched). The app-facing constants thread the user chose in Phase 183 is complete: the four carrier kinds are drawable (184), askable and readable (185).

- **The reader is `entails` stopped one step early.** `constantOf` on both closures runs the same reduction and returns the residual constant instead of testing it. The one subtlety was a sign: `AngleEquation` keeps its constant on the right (`Σ c·θ ≡ k`, so the variable part is `−k`), `LengthEquation` on the left (`Σ c·l + k = 0`, so it is `k`). The first draft negated both and the length tests said `2/5` for `5/2`. PLAN §"The reader" records which side negates and why.
- **The ℤ-module line held, and it is now pinned twice.** `constantOf` refuses the non-divisible step `entails` refuses: a closure holding `2Δθ ≡ 0` reads no `Δθ` — the disjunction stays a disjunction, and the reader will not pick the root the figure sits on. Test in `angle_closure_test`. `antilog` is the length side's refusal: `{2: ½}` is `√2`, carried exactly and stated by nothing.
- **A reading is never a verdict.** `ProverNotifier.read` runs to the fixpoint and leaves the run held as the state; the value goes back to the draft, and the ordinary *Ask* proves it with a certificate. `readConstant` records nothing (pinned: database length unchanged across a read).
- **The value rides on the draft beside the slots.** Three templates, `QuestionDraft.value`/`withValue`/`needsValue`/`constantSpellings`; the bar's field takes degrees for an angle and hands the draft the residue (−120 is the 60° line), *Read* fills it, an undetermined read says so in a snackbar. Out-of-range values are "no question", never a throw from a rebuild.
- **A latent test-ordering fragility surfaced**: `ProofPanel.dispose` schedules `proofHighlightProvider.clear()` post-frame, and a tree torn down together with its container (the next test's first frame) hit a disposed ref. `clear` now checks `ref.mounted`. It had never fired because no test followed the last "on a phone" test until this phase added one.

**Next session: Phase 186** — the kernel track resumes; a decision with the user among the named open items (toolchain upgrade recommended first, since every other item is measured against goldens and a renderer the upgrade moves). Carried: `readFact` magnitude-false; the skeptical re-glance; Phase 176 trigger; `regular_hexagon#2`'s asterisk.

**Gotchas.**

- **The two AR equation types keep their constant on opposite sides.** Any new reader, chase or renderer that touches both must check the sign against a stated value, not derive it from the other side's code. The `5/2` vs `2/5` failure is the cheap way to find out.
- **A `const Key` on a wrapper widget is not a key on its `TextField`** — `tester.widget<TextField>(find.byKey(...))` casts the wrapper and throws; find the field as a descendant.
- **`ref.read` from a post-frame callback scheduled in `dispose`** can run after the container is gone. Guard with `ref.mounted` in the notifier, not in the widget.


## 2026-09-02 — session 184: Phase 187, the toolchain upgrade — Flutter 3.41.9 → 3.47.2, and it cost less than the deferral feared

**Done — Phase 186's decision box ticked (the toolchain first, as recommended), Phase 187 opened, built and closed: six commits on `phase-187-toolchain`.** Suite **3541** (unchanged: 3509 + 32 goldens), analyze clean, browser gate 15, wasm release build compiles. No behaviour change anywhere; the corpus is untouched.

- **The SDK moved to what CI had been running.** `flutter upgrade` on the stable clone landed on `d3b14c8`, exactly the `stable-3.47.2` the last CI run resolved — Flutter 3.47.2 / Dart 3.13.2. `environment.sdk` raised to `^3.13.0`.
- **The reformat was 62 files, and the sdk floor is what sized it.** A `dart format` dry run under the new SDK but the old floor touched 16 files; after the floor moved to 3.13 it touched 62. The formatter's rules are gated on the *language version*, not the tool version — object-pattern and argument-list splitting came in with 3.13. Own commit, per the Phase 120c rule.
- **The pins released.** `flutter pub upgrade --major-versions`: 40 packages, one constraint change (`freezed` ^3 → ^4; the lock had held a `3.2.6-dev.1` prerelease). `riverpod` 3.4.2, `riverpod_generator` 4.0.8, `analyzer` 13.3.0. `build_runner` regenerated ten providers and one freezed class; freezed 4's `==`/`hashCode` read fields through a local self-cast, no API change. `file_picker_platform_interface` 3.3.0 added `darwinOptions` to `pickFile` and `PlatformFile.lengthSync`, which broke the test fake and nothing in `lib/`; the fake grew both.
- **One lint, eight sites.** `prefer_initializing_formals` started firing because Dart 3.13 admits a private field as a *named* initializing formal — `Construction({this._kernel = const DocumentKernel()})` is exposed as `kernel:`. Applied everywhere it asked (two in `lib/domain/`, six in test stubs).
- **Four of 32 goldens moved, and the diff is one shape.** markers and measures, dark and light, 93–114 px each (0.03–0.04 %). Every differing pixel lies on the top and right edges of the right-angle square — the two edges not overdrawn by the angle's arms — with anti-aliasing weight moving between the rows either side of the stroke centre and the centre row unchanged. A sub-pixel rasterizer shift on a stroked axis-aligned path; nothing in our arithmetic. Regenerated; the other 28 are byte-identical.
- **PLAN §"Deferred decisions" rewritten**: the pin is now 3.47.2 / 3.13.2, and the rule for the next upgrade is stated — it is due when CI's `stable` moves past the local SDK, and a red CI on an untouched tree is the signal the interval has run out.

**Next session: merge, then the deployed look** (the one open box — the renderer moved and the wasm build only proves it compiles). Then Phase 186's next decision among the three kernel items, oldest first: the chart-free line orientation (Phase 137's left-open), the join budget unit (Phase 161's box), the locus starve (Phase 139's left-open). Carried, unchanged: `readFact` magnitude-false; the skeptical re-glance; the Phase 176 trigger (3.0.1 as of today); `regular_hexagon#2`'s asterisk.

**Gotchas.**

- **Raise the sdk floor *before* the format dry run**, or the dry run under-reports. The formatter keys its rules on the language version the floor sets.
- **The first browser-gate run after the SDK move failed to compile** — `Undefined name 'main'` in the generated `test_0` harness for `test/web/` — and the immediate re-run was clean. A stale web test cache from the old SDK is the likely cause; if it recurs, `flutter clean` before believing it.
- **Do not run two `flutter test` processes at once**; they share `.dart_tool/` and the build cache. The suite ran in the background while the goldens ran in the foreground and got away with it, but the browser gate is a compile and would not.
- **`flutter pub get` under 3.47 edits `analysis_options.yaml` on its own** (adds `build/`, `android/`, `ios/`, `web/` excludes). Commit it; it comes back otherwise.

## 2026-09-03 — session 185: Phase 188, the join budget unit — measured, and it was not a join

**Done — Phase 186's next kernel item taken up as Phase 188, built and closed: three commits on `phase-188-join-budget-unit`, plus this docs commit.** Suite **3541 → 3549** (+8), analyze clean, browser gate 15, goldens byte-identical (no rendering change). Corpus byte-identical before and after, and 3.3× faster.

- **Phase 186's list was one item too long, and the docs said so first.** It named "the chart-free line orientation" as Phase 137's left-open, but Phase 137 *was* that phase; the unticked box was Phase 136c's, stale since session 145. Ticked with the pointer, Phase 186's line corrected, and the next genuinely open item — Phase 161's unit decision — became this phase.
- **The engine now tallies its uncharged half.** `ProverEngine.tallies` / `Prover.tallies`, per rule: *visits* (every binding attempt, one per spelling of every fact a slot was joined over), *applications*, *derived*. `RuleTally` is a value; two reads diffed isolate a pass. `benchmark/prover_join_bench.dart` reads them pass by pass at the provider's chunk and prints the worst pass's per-rule visits.
- **The measurement answered a different question from the one the box asked.** `tangent-chase`'s worst pass: 428 ms, 3.15 M visits, of which `aa_simtri` 92.6 % and `perp_from_inclination` 6.7 % — **zero applications between them**. Both rules' second premise is fully bound by their first, so the "join" was scanning every `eqangle` fact × 128 spellings to find a statement it could have named. `apatitos-topos`: the same two rules at 95 %.
- **The fix is a lookup and a screen, not a unit.** A fully bound premise is canonicalized to a `Fact` and probed in one map read, checked against the pivot's snapshot so the semi-naive argument stands; a partially bound premise skips any fact not carrying every bound point before its orbit is built. Same candidates, same order; the only change in `applications` is the disappearance of duplicates (a fully bound match once per equal spelling — `aa_simtri` on `provoleas2` 96 → 48). Pinned with exact visit counts (96 under `orthocentre`, 192 under `perp_bisector`), each mutation-checked to fail on exactly its own knockout.
- **Result**: worst warm pass at chunk 250, VM, idle — `tangent-chase` 14–19 ms, `apatitos-topos` 12–35, `provoleas2` 17–23, `perp-true-unproved` 14. DD alone costs the same as DD + AR, so the remainder is the charged half: 250 applications at 50–100 µs, the filter's probe. The unit the budget counts is already the unit of the freeze; Phase 161's box is ticked with neither of its options taken. PLAN §"A fully bound premise is a lookup, not a join".

**Next session: merge (this docs commit closes the phase), check `gh run list`, then the deployed look — Phase 187's one open box is still open, the Chrome extension was unconnected again this session (fourth occurrence).** Then Phase 186's last kernel item: the locus starve (Phase 139's left-open, a pass that declines a detour it cannot afford). Carried, unchanged: `readFact` magnitude-false; the skeptical re-glance; the Phase 176 trigger; `regular_hexagon#2`'s asterisk.

**Gotchas.**

- **A timing bench taken while `flutter test` runs in the background is not a measurement.** Two of this session's "after" numbers were taken that way and were 2× pessimistic; every number recorded above was re-taken on an idle machine. Counts (visits, applications, facts) are deterministic and unaffected — only the milliseconds lie.
- **A pin on the lookup can be satisfied by the screen alone.** The first draft used an incentre's eqangles against a disjoint orthocentre's perps: the screen rejected every perp before the probe mattered, and disabling the lookup failed nothing. The pin has to put facts of the premise's kind over the *same* point set, so the screen passes them all and only the probe keeps the count at one.
- **`zsh` expands a bare `=====` as `=cmd` path lookup** — `echo ======` in a Bash step fails with "not found". Use `---` as a separator.
- **The corpus bench prints only at the end** (it collects results, then tables them), so an empty output file mid-run means nothing. ~2 min now, was ~6.

## 2026-09-03 — session 185 (continued): the browser checks, and the rotation that was five sessions late

**Done — docs only, on `main`.** The Chrome extension connected once the user opened Chrome, so the two browser boxes that had waited through four unconnected sessions were run, and the rotation both file headers ask for was done.

- **Phase 187's deployed look, ticked.** The deployed build (wasm 2 821 455 bytes decoded, up from the Phase 163 build's 2 727 323) loaded the v1 kitchen-sink document through the file-input patch; fitted to view, every marker kind the moved goldens cover — the right-angle square, angle arcs, a sector fill — plus measures and labels rendered coherently. Console clean.
- **Phase 157's real-browser look, ticked.** A debug build (`flutter build web --debug`, served by `python3 -m http.server 8731` from `build/web`), `tangent-chord.rgl`, prover run from the panel: raw tooltip on hover after the wait, wheel-scroll and row-to-row hover under it, nothing logged. A sentinel `console.error` proved the capture was live before the empty result was believed.
- **Phase 154's narrow window stays open, for the same reason as before**: the page reported `outerWidth` 733 against `innerWidth` 1728 — Chrome in macOS fullscreen, where `resize_window` no-ops. Needs the window out of fullscreen first.
- **Rotation.** `docs/STATUS.md` 2149 → 373 lines: sessions 108–175 moved to `docs/archive/STATUS-sessions-108-175.md`, whose header explains the two conventions that had coexisted (a newest-first block and an appended block, with colliding session numbers 160–164 — read the date). The live header now says entries are appended, which is what every session since 163 has done. `docs/TODO.md` 1053 → 127 lines: 66 fully-ticked phases (121–185) appended to `docs/archive/TODO-completed-phases.md` in phase order; the four titled "(open)" with every box ticked (132, 134, 152, 161) retitled with where their open half went. What stays live: the V1 carry-overs, 188, 187, 186, 176, and the five phases with a genuinely open box (154, 157 now ticked but kept this once, 139, 137, 132d).

**Next session: Phase 186's last kernel item, the locus starve** (Phase 139's left-open). Carried: Phase 154's narrow-window look (leave fullscreen first); `readFact` magnitude-false; the skeptical re-glance; the Phase 176 trigger; `regular_hexagon#2`'s asterisk.

**Gotchas.**

- **Flutter's semantics tree is empty until "Enable accessibility" is pressed, and it stayed empty after** — `find` and `read_page` see only the host containers. Locate toolbar buttons by hovering for the tooltip and zooming, not by the accessibility tree. The Proof button is the checklist icon; the panel's play button (top right of the panel) runs the prover.
- **The toolbar's x-positions depend on the viewport width**: the deployed tab was 1465 px wide and the local one 1499, and a click copied from one missed the folder icon on the other. Screenshot before clicking on a new tab.
- **`echo ======` fails in zsh** (`=cmd` expansion) and so does an unquoted `--include=*.dart` with no match in the cwd (`no matches found`). Quote globs or use `---`.
- **The narrow-window tell is still `outerWidth < innerWidth`**, and the memory note about it saved a wasted attempt.

## 2026-09-05 — session 186: M-3D planned on its own branch; Phase 189, a pass keeps what it carried across

**Done.** Two things, on two branches.

- **`m-3d-planning` (not merged): the M-3D0 decision record.** The user asked whether the app is ready to start 3D and for the docs to plan it imminently. It is — every gate the plan set (kernel track, M-CK, M-P's plateau, the toolchain) is closed — so PLAN §"M-3D — three dimensions" now replaces the five-line outline with the record written before any code, specified by one fixture: the pyramid OABCD with a square base of side a and equilateral lateral faces, read for ∠OAC (45°), the base-to-face dihedral (arctan √2) and the face-to-face dihedral (arccos(−1/3)) under a drag of a base vertex. Seven concrete kinds under four abstract ones (`GeoPoint3` / `GeoLine3` / `GeoPlane` / `GeoSphere`), `projPoint3` total on every point with the workplane as the chart, real-first types with `Complex` only in line ∩ sphere, no 3D tracing and a pinned branch rule, line ∩ line in space excluded with quadric ∩ quadric, dihedral angles as a kind, the camera in presentation with the V1 rotation as its roll, codec v4 only for a document carrying a 3D kind. Phases 190–196 in `docs/TODO.md`. The branch is one docs commit off `main`; merging it is Phase 190.
- **Phase 189 on `main`**, Phase 186's last kernel item. Phase 139's box asked for a pass to decline a detour it cannot afford; `benchmark/detour_cost_probe.dart` measured the detour in three parts (approach 47–93 trials, arc 4–19, exit 15–42) and found the budget runs out *past* the crossing — 35 trials into the transversal's 42-trial exit — with the identity already carried. So the pass keeps it: exhaustion after a crossing it can vouch for (a measured collision, or a realness flip across the arc) ends the pass at its last accepted parameter with branches adopted and `reached < 1`; the sessions anchor there, the point lags the pointer for a frame, the next frame finishes. At 128 the transversal rig's G is carried where it was lost. A pass with no crossing still throws. PLAN §"A pass keeps what it carried across".

**Next session: Phase 190** — merge `m-3d-planning` into `main` (rebase it first; it predates this entry), then Phase 191, the kernel types under `lib/domain/projective/space/`. Carried: Phase 154's narrow-window look; `readFact` magnitude-false; the skeptical re-glance; the Phase 176 trigger; `regular_hexagon#2`'s asterisk.

**Gotchas.**

- **The first cut kept every detour and the ultra-tight near-miss test caught it**: an arc planned from the extrapolated estimate around a 1e-10 near-miss winds around nothing, and keeping its stop would have let the next frames creep into the miss with arcs that might. Certification by the *measurement* alone then lost every tangency, because the √ law lets the creep come within 1e-5 of t* — inside `locateSeparationMinimum`'s first probe, which answers null — so tangencies are always extrapolated. The realness flip is what certifies them.
- **A bailed frame reports `reached: 0`**, so "kept" is `!bailed && reached < 1`; the first draft of the session test counted the endpoint-singularity bail as a kept frame.
- **Counts in the new tests are found, not pinned**: the regime tests scan budgets upward for the first keep rather than hard-coding 74, because the plan point moves with any change to the trigger or the measurement. The session test pins 96 on a regime 84–108 wide.

